import { SafeAreaView } from "react-native-safe-area-context"


const ActivityLog = () => {
    return (
        <SafeAreaView>

        </SafeAreaView>
    );
};

export default ActivityLog;